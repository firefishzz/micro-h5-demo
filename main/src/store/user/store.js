import * as mutations from './mutations'
import * as actions from './actions'
const state = {
  userInfo: {}
}
const getters = {}
const user = {
  state,
  getters,
  mutations,
  actions
}
export default user
